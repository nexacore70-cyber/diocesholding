# Optional backend fallback for languages that the browser cannot translate.
# Never expose a cloud translation provider's secret key in a VITE_ variable.
VITE_TRANSLATION_API_URL=https://api.yourdomain.com/api/translate
